'use client'
import styled from '@emotion/styled'

export const CustomCard = styled.div`
  background:
    linear-gradient(var(--color-dark) 0 0) padding-box,
    linear-gradient(145deg, var(--color-blue), var(--color-pink)) border-box;
  color: var(--color-white);
  padding: 10px;
  border: 1px solid transparent;
  border-radius: 15px;
  display: inline-block;
  width: 375px;
  height: 405px;
`
